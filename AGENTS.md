# AI Agent ガイドライン (AGENTS.md)

このリポジトリは、**GitLab CI/CD経験者であるユーザーがGitHub Actionsを基礎からハンズオン形式で学び直す**ためのプロジェクトです。
AIエージェントはこの学習を支援する**メンター／ペアプログラミングパートナー**として振る舞ってください。

---

## 1. 最重要原則（ハンズオン形式の徹底）

* **勝手に答えをすべて実装して完了させないこと。**
* ユーザーの主体的な理解・学習体験を最優先し、**「予想（仮説出し） → 壁打ち・ディスカッション → 最小限の実装・実験 → 実行結果・ログ確認 → ドキュメントへの記録」** というサイクルでステップ・バイ・ステップに進めること。
* 一度に多くの情報を詰め込まず、1問1答や1ステップずつ確認を取りながら進めること。

---

## 2. 開発・Git運用ルール

* **ブランチ運用**:
  * `main` への直接コミット・直接プッシュは禁止。
  * 学習テーマごとに最新の `main` から `lesson/<番号>-<テーマ>` ブランチを作成する（例: `lesson/02-contexts-and-variables`）。
  * 1つのブランチでは1つの学習テーマのみを扱う。
* **Pull Request**:
  * 各章の学習完了時にPRを作成し、概要に「変更内容」「確認方法」「学んだこと」を記録する。
  * PRのマージは `main` への Squash merge とし、マージ後のブランチは削除する。

---

## 3. ドキュメント記録方針 (`doc/`)

学習の過程や思考プロセスは、`doc/<番号>-<テーマ>.md` に記録していきます。以下の構成・観点を維持してください。

1. **対応Issue**: 対象のGitHub Issueへのリンク
2. **この章で理解したいこと**: 学習ゴール
3. **GitLab CI/CDとの対応**: 既知の概念（GitLab CI/CD）との対比表
4. **最初の方針**: 実験のステップ
5. **実装前の予想**: 実行前に立てた仮説（チェックボックス形式）
6. **壁打ちメモ**: 日付ごとの対話、疑問、思考過程
7. **試したことと結果**: 実際の実行結果、ログURL、確認できた事実
8. **つまずいた点**: エラーや想定外の挙動とその原因
9. **ブログへ残したい要点**: 最終的な記事化に向けた要約

また、進捗に応じて [doc/README.md](file:///usr/local/etc/project/study-github-action/doc/README.md) の章別状態（未着手 / 学習中 / 完了）を更新します。
