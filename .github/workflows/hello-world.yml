name: "chap1"

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
jobs:
  hello:
    runs-on: "ubuntu-latest"
    steps:
      - name: "Show event context"
        run: |
          echo "event: ${{ github.event_name }}"
          echo "branch: ${{ github.ref }}"
