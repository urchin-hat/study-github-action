name: "chap1"

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/**"
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/**"
  workflow_dispatch:
jobs:
  hello:
    runs-on: "ubuntu-latest"
    steps:
      - name: "Show event context"
        run: |
          echo "event: ${{ github.event_name }}"
          echo "branch: ${{ github.ref }}"
