name: "chap1"

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
jobs:
  hello:
    runs-on: "ubuntu-latest"
    steps:
      - name: "Hello, World"
        run: |
          echo "hello,world"
